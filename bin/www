#!/usr/bin/env node

var app = require('./../app');
var port = process.env.PORT || 3000; 
var mongoose = require('mongoose');

mongoose.connect("mongodb://localhost:27017/shortener-dev", done);

app.listen(port, function() {
  console.log("Listening on port " + port);
});


